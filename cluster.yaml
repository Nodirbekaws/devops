apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: react-devops-cluster
  region: us-east-1

managedNodeGroups:
  - name: react-workers
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 1
    maxSize: 2